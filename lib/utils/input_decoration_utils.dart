import 'package:flutter/material.dart';

OutlineInputBorder defaultBorder(Color color) => OutlineInputBorder(
  borderRadius: BorderRadius.circular(10.0),
  borderSide: BorderSide(color: color, width: 1.5),
);
